'use strict';

var octiconsReact = require('@primer/octicons-react');
var clsx = require('clsx');
var React = require('react');
var TreeView_module = require('./TreeView.module.css.js');
var ConfirmationDialog = require('../ConfirmationDialog/ConfirmationDialog.js');
var _VisuallyHidden = require('../_VisuallyHidden.js');
var useControllableState = require('../hooks/useControllableState.js');
var useId = require('../hooks/useId.js');
var useSafeTimeout = require('../hooks/useSafeTimeout.js');
var useSlots = require('../hooks/useSlots.js');
var shared = require('./shared.js');
var useRovingTabIndex = require('./useRovingTabIndex.js');
var useTypeahead = require('./useTypeahead.js');
var SkeletonAvatar = require('../experimental/Skeleton/SkeletonAvatar.js');
var SkeletonText = require('../experimental/Skeleton/SkeletonText.js');
var jsxRuntime = require('react/jsx-runtime');
var Spinner = require('../Spinner/Spinner.js');
var Text = require('../Text/Text.js');

function _interopDefault (e) { return e && e.__esModule ? e : { default: e }; }

var React__default = /*#__PURE__*/_interopDefault(React);

const RootContext = /*#__PURE__*/React__default.default.createContext({
  announceUpdate: () => {},
  expandedStateCache: {
    current: new Map()
  }
});
const ItemContext = /*#__PURE__*/React__default.default.createContext({
  itemId: '',
  level: 1,
  isSubTreeEmpty: false,
  setIsSubTreeEmpty: () => {},
  isExpanded: false,
  setIsExpanded: () => {},
  leadingVisualId: '',
  trailingVisualId: ''
});

// ----------------------------------------------------------------------------
// TreeView

/* Size of toggle icon in pixels. */
const TOGGLE_ICON_SIZE = 12;
const Root = ({
  'aria-label': ariaLabel,
  'aria-labelledby': ariaLabelledby,
  children,
  flat,
  truncate = true,
  className,
  style
}) => {
  const containerRef = React__default.default.useRef(null);
  const mouseDownRef = React__default.default.useRef(false);
  const [ariaLiveMessage, setAriaLiveMessage] = React__default.default.useState('');
  const announceUpdate = React__default.default.useCallback(message => {
    setAriaLiveMessage(message);
  }, []);
  const onMouseDown = React.useCallback(() => {
    mouseDownRef.current = true;
  }, []);
  React.useEffect(() => {
    function onMouseUp() {
      mouseDownRef.current = false;
    }
    document.addEventListener('mouseup', onMouseUp);
    return () => {
      document.removeEventListener('mouseup', onMouseUp);
    };
  }, []);
  useRovingTabIndex.useRovingTabIndex({
    containerRef,
    mouseDownRef
  });
  useTypeahead.useTypeahead({
    containerRef,
    onFocusChange: element => {
      if (element instanceof HTMLElement) {
        element.focus();
      }
    }
  });
  const expandedStateCache = React__default.default.useRef(null);
  if (expandedStateCache.current === null) {
    expandedStateCache.current = new Map();
  }
  return /*#__PURE__*/jsxRuntime.jsx(RootContext.Provider, {
    value: {
      announceUpdate,
      expandedStateCache
    },
    children: /*#__PURE__*/jsxRuntime.jsxs(jsxRuntime.Fragment, {
      children: [/*#__PURE__*/jsxRuntime.jsx(_VisuallyHidden, {
        role: "status",
        "aria-live": "polite",
        "aria-atomic": "true",
        children: ariaLiveMessage
      }), /*#__PURE__*/jsxRuntime.jsx("ul", {
        ref: containerRef,
        role: "tree",
        "aria-label": ariaLabel,
        "aria-labelledby": ariaLabelledby,
        "data-omit-spacer": flat,
        "data-truncate-text": truncate || false,
        onMouseDown: onMouseDown,
        className: clsx.clsx(className, TreeView_module.TreeViewRootUlStyles),
        style: style,
        children: children
      })]
    })
  });
};
Root.displayName = "Root";
Root.displayName = 'TreeView';

// ----------------------------------------------------------------------------
// TreeView.Item

const Item = /*#__PURE__*/React__default.default.forwardRef(({
  id: itemId,
  containIntrinsicSize,
  current: isCurrentItem = false,
  defaultExpanded,
  expanded,
  onExpandedChange,
  onSelect,
  children,
  className,
  'aria-label': ariaLabel,
  'aria-labelledby': ariaLabelledby
}, ref) => {
  const [slots, rest] = useSlots.useSlots(children, {
    leadingAction: LeadingAction,
    leadingVisual: LeadingVisual,
    trailingVisual: TrailingVisual
  });
  const {
    expandedStateCache
  } = React__default.default.useContext(RootContext);
  const labelId = useId.useId();
  const leadingVisualId = useId.useId();
  const trailingVisualId = useId.useId();
  const [isExpanded, setIsExpanded] = useControllableState.useControllableState({
    name: itemId,
    // If the item was previously mounted, its expanded state might be cached.
    // We check the cache first, and then fall back to the defaultExpanded prop.
    // If defaultExpanded is not provided, we default to false unless the item
    // is the current item, in which case we default to true.
    defaultValue: () => {
      var _ref, _expandedStateCache$c, _expandedStateCache$c2;
      return (_ref = (_expandedStateCache$c = (_expandedStateCache$c2 = expandedStateCache.current) === null || _expandedStateCache$c2 === void 0 ? void 0 : _expandedStateCache$c2.get(itemId)) !== null && _expandedStateCache$c !== void 0 ? _expandedStateCache$c : defaultExpanded) !== null && _ref !== void 0 ? _ref : isCurrentItem;
    },
    value: expanded === null ? false : expanded,
    onChange: onExpandedChange
  });
  const {
    level
  } = React__default.default.useContext(ItemContext);
  const {
    hasSubTree,
    subTree,
    childrenWithoutSubTree
  } = useSubTree(rest);
  const [isSubTreeEmpty, setIsSubTreeEmpty] = React__default.default.useState(!hasSubTree);
  const [isFocused, setIsFocused] = React__default.default.useState(false);

  // Set the expanded state and cache it
  const setIsExpandedWithCache = React__default.default.useCallback(newIsExpanded => {
    var _expandedStateCache$c3;
    setIsExpanded(newIsExpanded);
    (_expandedStateCache$c3 = expandedStateCache.current) === null || _expandedStateCache$c3 === void 0 ? void 0 : _expandedStateCache$c3.set(itemId, newIsExpanded);
  }, [itemId, setIsExpanded, expandedStateCache]);

  // Expand or collapse the subtree
  const toggle = React__default.default.useCallback(event => {
    setIsExpandedWithCache(!isExpanded);
    event === null || event === void 0 ? void 0 : event.stopPropagation();
  }, [isExpanded, setIsExpandedWithCache]);
  const handleKeyDown = React__default.default.useCallback(event => {
    switch (event.key) {
      case 'Enter':
      case ' ':
        if (onSelect) {
          onSelect(event);
        } else {
          toggle(event);
        }
        event.stopPropagation();
        break;
      case 'ArrowRight':
        // Ignore if modifier keys are pressed
        if (event.altKey || event.metaKey) return;
        event.preventDefault();
        event.stopPropagation();
        setIsExpandedWithCache(true);
        break;
      case 'ArrowLeft':
        // Ignore if modifier keys are pressed
        if (event.altKey || event.metaKey) return;
        event.preventDefault();
        event.stopPropagation();
        setIsExpandedWithCache(false);
        break;
    }
  }, [onSelect, setIsExpandedWithCache, toggle]);
  const ariaDescribedByIds = [slots.leadingVisual ? leadingVisualId : null, slots.trailingVisual ? trailingVisualId : null].filter(Boolean);
  return /*#__PURE__*/jsxRuntime.jsx(ItemContext.Provider, {
    value: {
      itemId,
      level: level + 1,
      isSubTreeEmpty,
      setIsSubTreeEmpty,
      isExpanded,
      setIsExpanded: setIsExpandedWithCache,
      leadingVisualId,
      trailingVisualId
    },
    children: /*#__PURE__*/jsxRuntime.jsxs("li", {
      className: clsx.clsx('PRIVATE_TreeView-item', className, TreeView_module.TreeViewItem),
      ref: ref,
      tabIndex: 0,
      id: itemId,
      role: "treeitem",
      "aria-label": ariaLabel,
      "aria-labelledby": ariaLabel ? undefined : ariaLabelledby || labelId,
      "aria-describedby": ariaDescribedByIds.length ? ariaDescribedByIds.join(' ') : undefined,
      "aria-level": level,
      "aria-expanded": isSubTreeEmpty && (!isExpanded || !hasSubTree) || expanded === null ? undefined : isExpanded,
      "aria-current": isCurrentItem ? 'true' : undefined,
      "aria-selected": isFocused ? 'true' : 'false',
      "data-has-leading-action": slots.leadingAction ? true : undefined,
      onKeyDown: handleKeyDown,
      onFocus: event => {
        var _event$currentTarget$;
        // Scroll the first child into view when the item receives focus
        (_event$currentTarget$ = event.currentTarget.firstElementChild) === null || _event$currentTarget$ === void 0 ? void 0 : _event$currentTarget$.scrollIntoView({
          block: 'nearest',
          inline: 'nearest'
        });

        // Set the focused state
        setIsFocused(true);

        // Prevent focus event from bubbling up to parent items
        event.stopPropagation();
      },
      onBlur: () => setIsFocused(false),
      onClick: event => {
        if (onSelect) {
          onSelect(event);
        } else {
          toggle(event);
        }
        event.stopPropagation();
      },
      onAuxClick: event => {
        if (onSelect && event.button === 1) {
          onSelect(event);
        }
        event.stopPropagation();
      },
      children: [/*#__PURE__*/jsxRuntime.jsxs("div", {
        className: clsx.clsx('PRIVATE_TreeView-item-container', TreeView_module.TreeViewItemContainer),
        style: {
          // @ts-ignore CSS custom property
          '--level': level,
          contentVisibility: containIntrinsicSize ? 'auto' : undefined,
          containIntrinsicSize
        },
        children: [/*#__PURE__*/jsxRuntime.jsx("div", {
          style: {
            gridArea: 'spacer',
            display: 'flex'
          },
          children: /*#__PURE__*/jsxRuntime.jsx(LevelIndicatorLines, {
            level: level
          })
        }), slots.leadingAction, hasSubTree ?
        /*#__PURE__*/
        // This lint rule is disabled due to the guidelines in the `TreeView` api docs.
        // https://github.com/github/primer/blob/main/apis/tree-view-api.md#the-expandcollapse-chevron-toggle
        // This has specific advice that the chevron be available only to pointer event.
        // If they take up a button role, they become unnecessary and numerous tab stops.
        jsxRuntime.jsx("div", {
          className: clsx.clsx('PRIVATE_TreeView-item-toggle', onSelect && 'PRIVATE_TreeView-item-toggle--hover', level === 1 && 'PRIVATE_TreeView-item-toggle--end', TreeView_module.TreeViewItemToggle, TreeView_module.TreeViewItemToggleHover, TreeView_module.TreeViewItemToggleEnd),
          onClick: event => {
            if (onSelect) {
              toggle(event);
            }
          },
          children: isExpanded ? /*#__PURE__*/jsxRuntime.jsx(octiconsReact.ChevronDownIcon, {
            size: TOGGLE_ICON_SIZE
          }) : /*#__PURE__*/jsxRuntime.jsx(octiconsReact.ChevronRightIcon, {
            size: TOGGLE_ICON_SIZE
          })
        }) : null, /*#__PURE__*/jsxRuntime.jsxs("div", {
          id: labelId,
          className: clsx.clsx('PRIVATE_TreeView-item-content', TreeView_module.TreeViewItemContent),
          children: [slots.leadingVisual, /*#__PURE__*/jsxRuntime.jsx("span", {
            className: clsx.clsx('PRIVATE_TreeView-item-content-text', TreeView_module.TreeViewItemContentText),
            children: childrenWithoutSubTree
          }), slots.trailingVisual]
        })]
      }), subTree]
    })
  });
});

/** Lines to indicate the depth of an item in a TreeView */
const LevelIndicatorLines = ({
  level
}) => {
  return /*#__PURE__*/jsxRuntime.jsx("div", {
    style: {
      width: '100%',
      display: 'flex'
    },
    children: Array.from({
      length: level - 1
    }).map((_, index) => /*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_TreeView-item-level-line', TreeView_module.TreeViewItemLevelLine)
    }, index))
  });
};
LevelIndicatorLines.displayName = "LevelIndicatorLines";
Item.displayName = 'TreeView.Item';

// ----------------------------------------------------------------------------
// TreeView.SubTree

const SubTree = ({
  count,
  state,
  children,
  'aria-label': ariaLabel
}) => {
  const {
    announceUpdate
  } = React__default.default.useContext(RootContext);
  const {
    itemId,
    isExpanded,
    isSubTreeEmpty,
    setIsSubTreeEmpty
  } = React__default.default.useContext(ItemContext);
  const loadingItemRef = React__default.default.useRef(null);
  const ref = React__default.default.useRef(null);
  const [loadingFocused, setLoadingFocused] = React__default.default.useState(false);
  const [subTreeLabel, setSubTreeLabel] = React__default.default.useState('');
  const previousState = usePreviousValue(state);
  const {
    safeSetTimeout
  } = useSafeTimeout();
  React__default.default.useEffect(() => {
    // If `state` is undefined, we're working in a synchronous context and need
    // to detect if the sub-tree has content. If `state === 'done` then we're
    // working in an asynchronous context and need to see if there is content
    // that has been loaded in.
    if (state === undefined || state === 'done') {
      if (!isSubTreeEmpty && !children) {
        setIsSubTreeEmpty(true);
      } else if (isSubTreeEmpty && children) {
        setIsSubTreeEmpty(false);
      }
    }
  }, [state, isSubTreeEmpty, setIsSubTreeEmpty, children]);

  // Handle transition from loading to done state
  React__default.default.useEffect(() => {
    const parentElement = document.getElementById(itemId);
    if (!parentElement) return;
    setSubTreeLabel(shared.getAccessibleName(parentElement));
    if (previousState === 'loading' && state === 'done') {
      var _ref$current;
      // Announce update to screen readers
      const parentName = shared.getAccessibleName(parentElement);
      if ((_ref$current = ref.current) !== null && _ref$current !== void 0 && _ref$current.childElementCount) {
        announceUpdate(`${parentName} content loaded`);
      } else {
        announceUpdate(`${parentName} is empty`);
      }

      // Move focus to the first child if the loading indicator
      // was focused when the async items finished loading
      if (loadingFocused) {
        const firstChild = useRovingTabIndex.getFirstChildElement(parentElement);
        if (firstChild) {
          safeSetTimeout(() => {
            firstChild.focus();
          });
        } else {
          safeSetTimeout(() => {
            parentElement.focus();
          });
        }
        setLoadingFocused(false);
      }
    } else if (state === 'loading') {
      const parentName = shared.getAccessibleName(parentElement);
      announceUpdate(`${parentName} content loading`);
    }
  }, [loadingFocused, previousState, state, itemId, announceUpdate, ref, safeSetTimeout]);

  // Track focus on the loading indicator
  React__default.default.useEffect(() => {
    function handleFocus() {
      setLoadingFocused(true);
    }
    function handleBlur(event) {
      // Skip blur events that are caused by the element being removed from the DOM.
      // This can happen when the loading indicator is focused when async items are
      // done loading and the loading indicator is removed from the DOM.
      // If `loadingFocused` is `true` when `state` is `"done"` then the loading indicator
      // was focused when the async items finished loading and we need to move focus to the
      // first child.
      if (!event.relatedTarget) return;
      setLoadingFocused(false);
    }
    const loadingElement = loadingItemRef.current;
    if (!loadingElement) return;
    loadingElement.addEventListener('focus', handleFocus);
    loadingElement.addEventListener('blur', handleBlur);
    return () => {
      loadingElement.removeEventListener('focus', handleFocus);
      loadingElement.removeEventListener('blur', handleBlur);
    };
  }, [loadingItemRef, state]);
  if (!isExpanded) {
    return null;
  }
  return /*#__PURE__*/jsxRuntime.jsxs("ul", {
    role: "group",
    style: {
      listStyle: 'none',
      padding: 0,
      margin: 0
    }
    // @ts-ignore Box doesn't have type support for `ref` used in combination with `as`
    ,
    ref: ref,
    "aria-label": ariaLabel || subTreeLabel,
    children: [state === 'loading' ? /*#__PURE__*/jsxRuntime.jsx(LoadingItem, {
      ref: loadingItemRef,
      count: count
    }) : children, isSubTreeEmpty && state !== 'loading' ? /*#__PURE__*/jsxRuntime.jsx(EmptyItem, {}) : null]
  });
};
SubTree.displayName = "SubTree";
SubTree.displayName = 'TreeView.SubTree';
function usePreviousValue(value) {
  const ref = React__default.default.useRef(value);
  React__default.default.useEffect(() => {
    ref.current = value;
  }, [value]);
  return ref.current;
}
const SkeletonItem = () => {
  return /*#__PURE__*/jsxRuntime.jsxs("span", {
    className: clsx.clsx(TreeView_module.TreeViewSkeletonItemContainerStyle, TreeView_module.TreeViewItemSkeleton, 'PRIVATE_TreeView-item-skeleton'),
    children: [/*#__PURE__*/jsxRuntime.jsx(SkeletonAvatar.SkeletonAvatar, {
      size: 16,
      square: true
    }), /*#__PURE__*/jsxRuntime.jsx(SkeletonText.SkeletonText, {
      className: TreeView_module.TreeItemSkeletonTextStyles
    })]
  });
};
SkeletonItem.displayName = "SkeletonItem";
const LoadingItem = /*#__PURE__*/React__default.default.forwardRef(({
  count
}, ref) => {
  const itemId = useId.useId();
  if (count) {
    return /*#__PURE__*/jsxRuntime.jsxs(Item, {
      id: itemId,
      ref: ref,
      children: [Array.from({
        length: count
      }).map((_, i) => {
        return /*#__PURE__*/jsxRuntime.jsx(SkeletonItem, {
          "aria-hidden": true
        }, i);
      }), /*#__PURE__*/jsxRuntime.jsxs("div", {
        className: clsx.clsx('PRIVATE_VisuallyHidden', TreeView_module.TreeViewVisuallyHidden),
        children: ["Loading ", count, " items"]
      })]
    });
  }
  return /*#__PURE__*/jsxRuntime.jsxs(Item, {
    id: itemId,
    ref: ref,
    children: [/*#__PURE__*/jsxRuntime.jsx(LeadingVisual, {
      children: /*#__PURE__*/jsxRuntime.jsx(Spinner, {
        size: "small"
      })
    }), /*#__PURE__*/jsxRuntime.jsx(Text, {
      className: "fgColor-muted",
      children: "Loading..."
    })]
  });
});
const EmptyItem = /*#__PURE__*/React__default.default.forwardRef((props, ref) => {
  return /*#__PURE__*/jsxRuntime.jsx(Item, {
    expanded: null,
    id: useId.useId(),
    ref: ref,
    children: /*#__PURE__*/jsxRuntime.jsx(Text, {
      className: "fgColor-muted",
      children: "No items found"
    })
  });
});
function useSubTree(children) {
  return React__default.default.useMemo(() => {
    const subTree = React__default.default.Children.toArray(children).find(child => /*#__PURE__*/React__default.default.isValidElement(child) && child.type === SubTree);
    const childrenWithoutSubTree = React__default.default.Children.toArray(children).filter(child => !(/*#__PURE__*/React__default.default.isValidElement(child) && child.type === SubTree));
    return {
      subTree,
      childrenWithoutSubTree,
      hasSubTree: Boolean(subTree)
    };
  }, [children]);
}

// ----------------------------------------------------------------------------
// TreeView.LeadingVisual and TreeView.TrailingVisual

const LeadingVisual = props => {
  const {
    isExpanded,
    leadingVisualId
  } = React__default.default.useContext(ItemContext);
  const children = typeof props.children === 'function' ? props.children({
    isExpanded
  }) : props.children;
  return /*#__PURE__*/jsxRuntime.jsxs(jsxRuntime.Fragment, {
    children: [/*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_VisuallyHidden', TreeView_module.TreeViewVisuallyHidden),
      "aria-hidden": true,
      id: leadingVisualId,
      children: props.label
    }), /*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_TreeView-item-visual', TreeView_module.TreeViewItemVisual),
      "aria-hidden": true,
      children: children
    })]
  });
};
LeadingVisual.displayName = 'TreeView.LeadingVisual';
const TrailingVisual = props => {
  const {
    isExpanded,
    trailingVisualId
  } = React__default.default.useContext(ItemContext);
  const children = typeof props.children === 'function' ? props.children({
    isExpanded
  }) : props.children;
  return /*#__PURE__*/jsxRuntime.jsxs(jsxRuntime.Fragment, {
    children: [/*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_VisuallyHidden', TreeView_module.TreeViewVisuallyHidden),
      "aria-hidden": true,
      id: trailingVisualId,
      children: props.label
    }), /*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_TreeView-item-visual', TreeView_module.TreeViewItemVisual),
      "aria-hidden": true,
      children: children
    })]
  });
};
TrailingVisual.displayName = 'TreeView.TrailingVisual';

// ----------------------------------------------------------------------------
// TreeView.LeadingAction

const LeadingAction = props => {
  const {
    isExpanded
  } = React__default.default.useContext(ItemContext);
  const children = typeof props.children === 'function' ? props.children({
    isExpanded
  }) : props.children;
  return /*#__PURE__*/jsxRuntime.jsxs(jsxRuntime.Fragment, {
    children: [/*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_VisuallyHidden', TreeView_module.TreeViewVisuallyHidden),
      "aria-hidden": true,
      children: props.label
    }), /*#__PURE__*/jsxRuntime.jsx("div", {
      className: clsx.clsx('PRIVATE_TreeView-item-leading-action', TreeView_module.TreeViewItemLeadingAction),
      "aria-hidden": true,
      children: children
    })]
  });
};
LeadingAction.displayName = 'TreeView.LeadingAction';
// ----------------------------------------------------------------------------
// TreeView.DirectoryIcon

const DirectoryIcon = () => {
  const {
    isExpanded
  } = React__default.default.useContext(ItemContext);
  const Icon = isExpanded ? octiconsReact.FileDirectoryOpenFillIcon : octiconsReact.FileDirectoryFillIcon;
  return /*#__PURE__*/jsxRuntime.jsx("div", {
    className: clsx.clsx('PRIVATE_TreeView-directory-icon', TreeView_module.TreeViewDirectoryIcon),
    children: /*#__PURE__*/jsxRuntime.jsx(Icon, {})
  });
};
DirectoryIcon.displayName = "DirectoryIcon"; // ----------------------------------------------------------------------------
// TreeView.ErrorDialog
const ErrorDialog = ({
  title = 'Error',
  children,
  onRetry,
  onDismiss
}) => {
  const {
    itemId,
    setIsExpanded
  } = React__default.default.useContext(ItemContext);
  return /*#__PURE__*/jsxRuntime.jsx("div", {
    onKeyDown: event => {
      if (['Backspace', 'ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown', 'Enter'].includes(event.key)) {
        // Prevent keyboard events from bubbling up to the TreeView
        // and interfering with keyboard navigation
        event.stopPropagation();
      }
    },
    children: /*#__PURE__*/jsxRuntime.jsx(ConfirmationDialog.ConfirmationDialog, {
      title: title,
      onClose: gesture => {
        // Focus parent item after the dialog is closed
        setTimeout(() => {
          const parentElement = document.getElementById(itemId);
          parentElement === null || parentElement === void 0 ? void 0 : parentElement.focus();
        });
        if (gesture === 'confirm') {
          onRetry === null || onRetry === void 0 ? void 0 : onRetry();
        } else {
          setIsExpanded(false);
          onDismiss === null || onDismiss === void 0 ? void 0 : onDismiss();
        }
      },
      confirmButtonContent: "Retry",
      cancelButtonContent: "Dismiss",
      children: children
    })
  });
};
ErrorDialog.displayName = "ErrorDialog";
ErrorDialog.displayName = 'TreeView.ErrorDialog';

// ----------------------------------------------------------------------------
// Export

const TreeView = Object.assign(Root, {
  Item,
  SubTree,
  LeadingAction,
  LeadingVisual,
  TrailingVisual,
  DirectoryIcon,
  ErrorDialog
});

exports.TreeView = TreeView;
